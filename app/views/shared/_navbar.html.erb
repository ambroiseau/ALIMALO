<nav class="navbar navbar-expand-lg navbar-dark bg-default container-fluid pl-6">
  <div id="alimlogo">
    <%= link_to(image_tag('alim-alo.png', class:"navbar-brand"), root_path) %>
  </div>

  <div class="defileParent">      
    <p class="defile" data-text=" ne manquez plus de rien le soir // ne manquez plus de rien tard le soir //ne manquez plus de rien le soir // ne manquez plus de rien tard le soir // ne manquez plus de rien le soir // ne manquez plus de rien tard le soir //ne manquez plus de rien le soir // ne manquez plus de rien tard le soir //">ne manquez plus de rien le soir // </p>
  </div>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><b>
        ALIMENTATION GENERALE</b></a>
        <div class="dropdown-menu" aria-labelledby="navbarNavDropdown">
          <a class="dropdown-item" href="/shop">LES ALIMS</a>
          <a class="dropdown-item" href="/">MAGAZINE ALIM</a>
          <a class="dropdown-item" href="/">RADIO ALIM</a>
          <a class="dropdown-item" href="/">À PROPOS D'ALIM-ALO</a>
        </div>

      <%if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><b>VOTRE ESPACE</b></a>
          <div class="dropdown-menu" aria-labelledby="navbarNavDropdown" id="espacepro">
						<% if shop_id = Shop.find_by(user_id: current_user.id) %>
						  <a data-toggle="modal" data-target="#signUpModal"><%= link_to "VOTRE BUREAU", shop_path(shop_id), class:"dropdown-item"%></a>
             <% else %>
							<a data-toggle="modal" data-target="#signUpModal"><%= link_to "VOTRE BUREAU", new_shop_path, { :user_id => current_user.id, class:"dropdown-item" } %></a>
							<%end%>
              <a data-toggle="modal" data-target="#logInModal"><%=  link_to "DECONNEXION", destroy_user_session_path, method: :delete, class:"dropdown-item" %></a>
              <a class="dropdown-item" href="/home/confidential">CGU</a>
            </div>

        <% else %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><b>ESPACE PRO</b></a>
            <div class="dropdown-menu" aria-labelledby="navbarNavDropdown" id="espacepro">
              <a class="dropdown-item" data-toggle="modal" data-target="#signUpModal">REJOINDRE</a>
              <a class="dropdown-item" data-toggle="modal" data-target="#logInModal">CONNEXION</a>
              <a class="dropdown-item" href="/home/confidential">CGU</a>
            </div>
          </li>
        <% end %>
      </li>
    </ul>
  </div>

</nav>
